# --- Core ---
aiogram==3.21.*       # >=3.20 поднимает верхнюю границу pydantic до <2.12
python-dotenv==1.0.*

# --- Async / HTTP ---
aiofiles==23.2.*      # aiogram требует ~=23.2.1
httpx==0.27.*

# --- Database (SQLite по умолчанию) ---
SQLAlchemy==2.0.*
aiosqlite==0.20.*

# --- Migrations ---
alembic==1.13.*

# --- Video Downloading ---
yt-dlp==2025.*
ffmpeg-python==0.2.*  # нужен установленный ffmpeg в PATH

# --- API / Server ---
fastapi==0.115.*
uvicorn==0.30.*

# --- Validation / Utils ---
pydantic==2.10.*      # >=2.8 совместим с Python 3.13
